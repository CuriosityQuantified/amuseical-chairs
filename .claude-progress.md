# Progress — amusical-chairs

## Current state (2026-07-19)
- `main` at `df5151c` (PR #4 merged: chairs tutorial in solo mode, slingshot screen-fit + high sensitivity `MAX_DRAG=110` / `AIM_PX_PER_RAD=55`).
- Format: score-attack — every player plays all 11 games once + musical-chairs reaction finale; normalized 0–1000 per game; highest total wins; no elimination.
- Dev server: `npm start` (port 3000). Client files are static — no restart needed for `public/` changes; restart for `server/` changes.

## Verified
- `npm test`: 36/36 pass, including the automated 20-bot harness (`test/harness.test.js`) with nosubmit/masher/flaky behaviors.
- Live 20-user integration mock against the running server (2026-07-19): host driver + 20 socket bots (4 groups of 5). All 11 games played exactly once, 20-row sorted scoreboard after every game, one chairs finale, winner = top of complete 20-player standings. Every bot (including the non-submitter) received exactly 12 personal `you:score` cards; bot-side totals/ranks matched host standings exactly. Scripts: scratchpad `live-host.mjs` / `live-bots.mjs`.

## Notable behaviors confirmed
- Non-submitters score 0 per game but stay in the game and still play (and can score in) the finale.
- Chairs tutorial shows in both solo and multiplayer (hosted `startChairsFinale()` wraps `startTutorial({key:'chairs'})`).

## Open items
- None pending; next work TBD by user.
